"""Setup script for editable installation."""

from setuptools import find_packages, setup

setup(
    name="polymarket-ai-trader",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies are in requirements.txt
    ],
)



